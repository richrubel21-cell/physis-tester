fastapi
uvicorn
sqlalchemy
pydantic
httpx
anthropic
python-dotenv
psycopg2-binary
# Playwright drives headless chromium for the functional test suite
# (Tests 37-46). render.yaml's buildCommand runs `playwright install
# --with-deps chromium` after pip so the browser is on disk before
# the worker boots.
playwright>=1.49.0

