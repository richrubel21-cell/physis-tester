fastapi
uvicorn
sqlalchemy
pydantic
httpx
anthropic
python-dotenv
psycopg2-binary
# Playwright drives headless chromium for the functional test suite
# (Tests 37-46). The image at mcr.microsoft.com/playwright/python:
# v1.49.0-jammy ships chromium pre-installed for playwright 1.49.0
# specifically — pinning the python package to the exact same version
# so pip can never resolve to a newer release whose driver expects
# a browser layout the image doesn't match.
playwright==1.49.0

